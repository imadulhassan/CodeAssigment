<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.CodeAssesment" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Toolbar style -->
    <style name="AppTheme.Toolbar" parent="Widget.MaterialComponents.Toolbar">
        <item name="android:background">?attr/colorPrimary</item>
        <item name="android:textColorPrimary">@android:color/white</item>
    </style>

    <!-- CollapsingToolbarLayout style -->
    <style name="AppTheme.CollapsingToolbar" parent="Widget.MaterialComponents.CollapsingToolbar">
        <item name="expandedTitleMarginStart">48dp</item>
        <item name="expandedTitleMarginEnd">64dp</item>
        <item name="contentScrim">?attr/colorPrimary</item>
        <item name="scrimAnimationDuration">600</item>
        <item name="scrimVisibleHeightTrigger">160dp</item>
    </style>

    <style name="Theme.CodeAssesment" parent="Base.Theme.CodeAssesment" />
</resources>